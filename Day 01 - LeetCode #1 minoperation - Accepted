class Solution:
    def minOperations(self, nums: List[int]) -> int:
        if len(nums) <= 1:
            return 0
        first_val = nums[0]
        for i in range(1, len(nums)):
            if nums[i] != first_val:
                return 1
        return 0
        